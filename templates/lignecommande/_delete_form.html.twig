<form method="post" action="{{ path('home-lignecommande_delete', {'id': lignecommande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lignecommande.id) }}">
    <button class="btn">Supprimer</button>
</form>

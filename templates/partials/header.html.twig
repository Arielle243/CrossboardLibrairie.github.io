

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/home.css') }}">
    {% endblock %}
    

            <div class="text-end">
                <div class="banner">
                    <div class="link">
                        {% if app.user %}

                            <div class="dropdown">
                                <button class="btn btn-secondary btn-sm dropdown-toggle" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i class="bi bi-person-circle"></i>{{ app.user.name }}
                                </button>
                               
                                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                                    {# ce lien sera accessible que par l'admin on vérifie si le user connecté à un rôle admin, on affiche ce lien.#}
                                    {% if is_granted('ROLE_ADMIN') %}
                                        <a class="dropdown-item" href="{{ path('admin') }}"><i class="bi bi-house-gear-fill"></i>Espace Admin</a>
                                         {# <a class="dropdown-item" href="{{ path('app_admin_compte_index') }}"><i class="bi bi-person-circle"></i>Les comptes</a> #}
                                    
                                     
                                         {% else %}
                                        <a class="dropdown-item" href="{{ path('client_index') }}"><i class="bi bi-person"></i>Mon compte</a>
                                    {% endif %}
                                        <a class="link" href="{{ path('app_logout') }}"><i class="bi bi-box-arrow-right"></i>Déconnexion</a>
                                </div>
                        
                                    
                                {% if is_granted('ROLE_CLIENT') %}
                                    <button type="button" class="btn btn-secondary btn-sm">
                                        <a class="link" href="{{ path('home-panier_checkout') }}"><i class="bi bi-basket"></i></a>
                                    </button>
                                {% endif %}
                            </div>
            
           

                            {% else %}

                            <button type="button" class="btn btn-secondary btn-sm">
                            <a  class="link" href="{{ path('app_login') }}"><i class="bi bi-person-circle"></i> Connexion</button><a>
           
                        {% endif %}
                    </div>
                  
                



                    <nav class="navbar navbar-expand-lg">
                        <div class="container-fluid">
                            <a class="navbar-brand" href="{{ path ('home-index') }}">
                                <img class="logo" src="{{ asset('upload/images/mesImages/logo2.png') }}" width="80" height="80"> Crossroard Librairie</a>
                                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"   aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                                <span class="navbar-toggler-icon"></button>
                                    <div class="collapse navbar-collapse" id="navbarText">
                                        <ul class="navbar-nav">
                                            <li class="nav-item active">
                                                <a class="nav-link" href="{{ path ('home-index') }}">
                                                    Accueil
                                                    <span class="sr-only">
                                                    (current)
                                                    </span>
                                                </a>
                                            </li>
                                            <li class="nav-item">
                                                <a class="nav-link" href="{{ path ('app_rayon') }}">Rayon</a>
                                            </li>

                                            <li class="nav-item">
                                                <a class="nav-link" href="#">
                                                    Évenements                                 
                                                </a>
                                            </li>
                                        </ul>
                                        <form class="d-flex">
                                            <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                                                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">
                                                    <i class="bi bi-search"></i>                              
                                                </button>
                                        </form>
                                    </div>
                        </div>
                    </nav>


                    {# Pour mettre en place l'image en banniere et récupérer l'utilisateur connecté en affichant son prénom par exemple d'abord on vérifie si l'utilisateur est connecté #}
         
                        {#  {% if app.user %} #}
                        {#     <div class="userIdentifier"> #}
                        {#         Vous êtes connecté en tant que  #}
                        {#             {{ app.user.firstname }}  #}
                        {#   </div> 
                 
                     {% endif %}#}
                                 
                </div> 

            </div>
                        
    

  
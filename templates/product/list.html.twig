{% for product in products %}
    {% include 'product/item.html.twig' with { product, leftCol: leftCol, rightCol: rightCol } %}
{% endfor %}

<div class="container">
    <div class="row">
        <div class="d-flex align-items-center justify-content-center">
            {{ knp_pagination_render(products) }}
        </div>
    </div>
</div>
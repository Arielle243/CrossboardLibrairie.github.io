<form  class="user_form_delete" method="post" action="{{ path('client_delete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete it?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-danger">Supprimer mon compte</button>
</form>
